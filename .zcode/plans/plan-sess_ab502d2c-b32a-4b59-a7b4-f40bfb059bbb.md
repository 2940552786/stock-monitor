## 实施计划：实时异动监控系统

### 1. server.py — 重写信号检测
- 删除所有旧的顶底检测代码（~230行，7条件+多空过滤+最高最低去重）
- 替换为事件检测器，逐分钟扫描（O(n)单遍）：
  - A. 价格异动：急拉/急跌(3分钟≥0.5%/0.8%/1.2%)、脉冲(单分钟≥0.3%)、突破今高/今低
  - B. 量价异动：放量涨/跌(量>P90)
  - C. VWAP事件：上穿/下穿均线
  - D. 多空信号：短多金叉/死叉、中多金叉/死叉
  - E. 盘口异动：委比急变>20%
- 去重：同类型事件3分钟内只保留最严重的
- 入队：按 `type+time` 去重，每只股票保留最近50条
- 推送适配：更新为 events 格式

### 2. index.html — 适配新格式
- `checkAllSignals()`: 适配 `events` 字段替代 `tops`/`bottoms`
- `renderSignalFeed()`: 每种事件类型独立颜色+图标标签
- 修复DOM累积bug（定时清理超过100条的旧行）
- 清理 `ob_filtered` 死代码

### 3. 验证+重启